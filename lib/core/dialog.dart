import 'package:flutter/material.dart';

class DialogMe extends StatelessWidget {
  const DialogMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
